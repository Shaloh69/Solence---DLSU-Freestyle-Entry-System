# data/

Gitignored dataset workspace populated by `scripts/`:

- `raw/<dataset>/` — untouched downloads
- `yolo/<dataset>/` — YOLO instance-seg conversions
- `unet/<dataset>/` — raster wall-mask conversions
- `_previews/` — rendered label overlays from `verify_dataset.py` for
  human review before training
