# --- Solence frontend (Next.js) ---

# Express API base URL (REST + /ws realtime)
NEXT_PUBLIC_API_URL=http://localhost:4000

# Supabase (client-side; auth integration pending)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
